# For developers: do NOT modify this file directly.
# Copy this file to ".env.local", and set the values there.

# For sysadmins: use this file as a guide for the environment
# variables which must be set on the target server/container
# before executing the service.

# For ENV, use either "dev" or "prod"

ENV=dev
NODE_PORT=3000
CORS_ORIGIN="http://localhost:3000"
MANAGERS_API_URL="https://o3m5qixdng.execute-api.us-east-1.amazonaws.com/api/managers"
MANAGERS_API_CACHE_MAX_AGE=600
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
